# Workflow

This folder contains the exported n8n workflow.

Import the JSON file into n8n to recreate the automation.

Before running the workflow, configure:

- Jira Cloud credentials
- OpenAI credentials
- Schedule Trigger (optional)
